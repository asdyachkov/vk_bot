paths:
  '':
    get:
      $handler: vk_bot_admin.games.views.get_users
      tags:
        - games
      summary: Games list
      description: Returns list of games

      produces:
        - application/json

      responses:
        200:
        description: OK